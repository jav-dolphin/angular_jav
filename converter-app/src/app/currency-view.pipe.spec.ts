import { CurrencyViewPipe } from './currency-view.pipe';

describe('CurrencyViewPipe', () => {
  it('create an instance', () => {
    const pipe = new CurrencyViewPipe();
    expect(pipe).toBeTruthy();
  });
});
